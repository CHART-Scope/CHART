name: Project Pages

on:
  pull_request:
    paths:
      - ".github/workflows/storybook-pages.yml"
      - ".prettierignore"
      - ".prettierrc.json"
      - "Makefile"
      - "docs/**"
      - "mkdocs.yml"
      - "package.json"
      - "package-lock.json"
      - "scripts/check_docs_links.py"
      - "web/.storybook/**"
      - "web/package.json"
      - "web/public/**"
      - "web/src/**"
      - "web/tsconfig.json"
  push:
    branches:
      - main
      - dev
    paths:
      - ".github/workflows/storybook-pages.yml"
      - ".prettierignore"
      - ".prettierrc.json"
      - "Makefile"
      - "docs/**"
      - "mkdocs.yml"
      - "package.json"
      - "package-lock.json"
      - "scripts/check_docs_links.py"
      - "web/.storybook/**"
      - "web/package.json"
      - "web/public/**"
      - "web/src/**"
      - "web/tsconfig.json"
  workflow_dispatch:

concurrency:
  group: chart-storybook-pages-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: Build project site
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check Storybook formatting
        run: npx prettier --check web/.storybook web/src package.json

      - name: Typecheck web
        run: npm run typecheck:web

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: docs/requirements.txt

      - name: Install documentation dependencies
        run: python -m pip install -r docs/requirements.txt

      - name: Configure Pages
        id: pages
        if: github.event_name != 'pull_request'
        uses: actions/configure-pages@v5

      - name: Build Storybook
        run: npm run build-storybook:web
        env:
          STORYBOOK_BASE_URL: ${{ steps.pages.outputs.base_path }}

      - name: Build documentation
        run: python -m mkdocs build --strict --site-dir web/storybook-static/docs

      - name: Check documentation links
        run: python scripts/check_docs_links.py web/storybook-static/docs

      - name: Upload project site
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v3
        with:
          path: web/storybook-static

  deploy:
    name: Deploy project site
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
      pages: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy Pages
        id: deployment
        uses: actions/deploy-pages@v4
